Our website is host in
`https://ipfs.smartup.global/ipfs/<hash>/`

## Setup
`yarn`

## Dev
`yarn start`

## Deploy
1. `ssh <user>@<ip>`
2. `cd ipfs/files`
3. Upload folder to here
4. `ipfs add -r <folder>`
